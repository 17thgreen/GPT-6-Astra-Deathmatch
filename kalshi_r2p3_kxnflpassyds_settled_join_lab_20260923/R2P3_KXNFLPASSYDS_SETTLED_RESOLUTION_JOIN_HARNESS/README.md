# R2P3 KXNFLPASSYDS settled-resolution join bundle

Byte copies of the attached freeze, scout reget, seed summary, panel
stub, settled reget, accept, frozen experiment, empty results, examiner
hold, maximize pin, and digest list. `admitted_at` stays null.
`results`, `pnl`, and `settled_join_n` stay null. Scout nonempty N=20
is a pin and is not copied into `settled_join_n`. This bundle does not
run `admit.py`.
