# Fixed follow-up selected after AMS-008 development screen

Five positive gross pair/size cases map to exactly two pairs: Mississippi cotton
800000/900000 and Teen Clothing ad spend 136/140. All are negative under the M=1
ordinary taker-fee benchmark. Verify the two current series fee records and obtain
fresh metadata/full books for the four fixed markets. No replacements. Also fetch
at most two pages of active volume incentives, limit 10000, to test whether the
combined gross price discrepancy plus public volume reward is even eligible.

The maximum public volume subsidy is $0.005 per eligible contract; a two-leg
pair's optimistic cap is $0.01 per paired unit only if both legs have an active
program and all consumed prices meet published eligibility. Actual pro-rata
reward and entitlement remain unknown. Report no-subsidy and upper-cap sensitivity;
never book the cap as realized or expected revenue. Missing program means do not
use a public reward to rescue this pair. Preserve catalog truncation.

Public GET only, no credential, four workers, 12-second timeout, no retries. Stop
after these fixed observations and report failures. Rule matching still does not
replace a complete settlement proof; no trades or P&L claims.
