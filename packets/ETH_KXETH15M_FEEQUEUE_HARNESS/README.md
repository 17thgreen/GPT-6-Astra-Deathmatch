# ETH harness packet copy

`lab/governance/astra/packets/` is absent in this checkout. These files are
the governance copy. Freeze, scout hunt, panel stub, and accept bytes match
the attached sha256 values. `SOURCE_PINS.json` lists digests and does not
contain those file bytes. `results` and `pnl` stay null.
