# Fixture pin

Sanitized Mechanic demo copies. Desk bytes were absent from this checkout, so
these files contain the stated keys only.

- `first_demo_queue_poll.json`: verdict `POLL_OK`, `queue_position_fp`
  `"4207.00"`, ticker `KXNFLGAME-26OCT01PITCLE-PIT`, clean cancel, fill absent,
  L2 top price `0.01` with size narrative `~4208`.
- `demo_queue_sample_series.json`: host `demo-api.kalshi.co` only,
  `n_success_this_run` 4, prior first included, `rows_total` 26,
  `leftover_resting` `no`.

No API keys, tokens, or account secrets. Calibration metrics stay null.
